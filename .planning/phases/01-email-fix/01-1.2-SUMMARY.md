---
plan: "1.2"
phase: 1
status: complete
completed: 2026-04-04
---

# Summary: Plan 1.2 — Approval Onboarding Email

## What Was Done

Rewrote the `resend.emails.send()` call in `api/approve-request.js` to use the shared helpers from `api/_email.js` (created in Plan 1.1) and deliver a full Polish onboarding email to newly approved CRA members.

Changes made:

1. Added `import { FROM, emailShell, ctaButton, esc } from './_email.js'` at the top of the file (after existing imports).
2. Replaced `from: 'onboarding@resend.dev'` with `from: FROM`.
3. Replaced the thin 4-line HTML body with a full onboarding template wrapped in `emailShell()`, containing:
   - Personalised Polish greeting using `esc(name)` (HTML injection prevention)
   - Approval confirmation paragraph welcoming the member to SSUEW
   - "Czym jest CRA?" section explaining the Central Administrative Register
   - "Jak się zalogować?" numbered login steps with link to cra-system.vercel.app
   - CTA button via `ctaButton('Przejdź do CRA →', 'https://cra-system.vercel.app')`
   - Closing "Do zobaczenia w systemie!" sign-off
4. All Firebase Admin initialisation, Firestore writes (`authorized_users.add`, `access_requests.update`), `res.status(200)` return, and `catch` block left completely unchanged.

## Files Modified

| File | Change |
|------|--------|
| `api/approve-request.js` | Added `_email.js` import; replaced sender and HTML body; +32 lines / -11 lines |

## Verification

All acceptance criteria confirmed via `grep` after writing the file:

| Criterion | Status |
|-----------|--------|
| `import { FROM, emailShell, ctaButton, esc } from './_email.js'` present | PASS (line 4) |
| `from: FROM` present | PASS (line 36) |
| `onboarding@resend.dev` absent | PASS (no match) |
| `html: emailShell(` present | PASS (line 39) |
| `esc(name)` present | PASS (line 40) |
| `ctaButton('Przejdź do CRA →', 'https://cra-system.vercel.app')` present | PASS (line 61) |
| "Jak się zalogować?" section present | PASS (line 53) |
| "Czym jest CRA?" section present | PASS (line 46) |
| `db.collection('authorized_users').add(` unchanged | PASS (line 26) |
| `db.collection('access_requests').doc(requestId).update({ status: 'approved' })` unchanged | PASS (line 32) |

Should-haves also verified:
- "katalog sprzętu", "rejestr stanowisk", "mapę kampusu" all present in CRA description paragraph.
- "Do zobaczenia w systemie!" present in closing.
- CTA button URL is exactly `https://cra-system.vercel.app`.

## Commit

`cc7fc94` — `feat(email): rewrite approve-request.js with full Polish onboarding email`

## Notes

- `esc()` is applied to `name` (from `req.body`) but not to `email` in the `to:` field — `email` is not interpolated into HTML, so no escaping is needed there. This matches the plan's instructions.
- The subject line emoji (`✅`) was preserved unchanged — intentional per plan spec.
- No deviations from the plan were required. The file was rewritten exactly as specified in task 1.2.1.

## Self-Check: PASSED

- `api/approve-request.js` exists and contains all required content (verified above).
- Commit `cc7fc94` exists: confirmed by git output during commit step.
