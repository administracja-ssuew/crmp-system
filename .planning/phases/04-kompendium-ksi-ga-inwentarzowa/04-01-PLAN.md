---
phase: 04-kompendium-ksi-ga-inwentarzowa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/KompendiumPage.jsx
autonomous: true
requirements:
  - KOMP-01

must_haves:
  truths:
    - "KompendiumPage renders a reading progress bar that updates as user scrolls"
    - "Every major section header (Wstęp, Słownik, Przed, Podczas, Po, Typy, Błędy) has a visually distinct color — not all violet"
    - "Each section begins with a 'Co znajdziesz tutaj' intro card listing 2–3 bullet points"
    - "sessionStorage key 'kompendium_checklist_v1' still persists checklist state across page reloads"
    - "Sticky nav and IntersectionObserver still work after changes"
  artifacts:
    - path: "src/pages/KompendiumPage.jsx"
      provides: "Redesigned KompendiumPage with progress bar, colored section headers, intro cards"
      contains: "readingProgress"
  key_links:
    - from: "window scroll event"
      to: "readingProgress state"
      via: "onScroll handler in useEffect"
      pattern: "readingProgress"
    - from: "SectionTitle color prop"
      to: "Tailwind icon wrapper classes"
      via: "colors lookup object inside SectionTitle"
      pattern: "colors\\[color\\]"
---

<objective>
Refactor KompendiumPage.jsx visual presentation: add a reading progress bar, change SectionTitle to accept a `color` prop so each section gets a distinct color, and add a `SectionIntro` component that renders a "Co znajdziesz tutaj" card at the start of each section.

Purpose: Requirement KOMP-01 — the page is a wall of text; adding visual structure makes it scannable and aesthetically coherent.
Output: Modified `src/pages/KompendiumPage.jsx` with progress bar, colored section headers, and intro cards. All checklist/nav/accordion logic untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-kompendium-ksi-ga-inwentarzowa/04-CONTEXT.md
@.planning/phases/04-kompendium-ksi-ga-inwentarzowa/04-RESEARCH.md

<interfaces>
<!-- Extracted from src/pages/KompendiumPage.jsx -->

Current SectionTitle signature (lines 354–366):
```jsx
const SectionTitle = ({ icon: Icon, chapter, title }) => (
  <div className="flex items-center gap-3 mb-6">
    {Icon && (
      <div className="w-10 h-10 bg-violet-100 rounded-xl flex items-center justify-center shrink-0">
        <Icon className="w-5 h-5 text-violet-600" />
      </div>
    )}
    <div>
      <p className="text-[10px] font-black uppercase tracking-widest text-violet-500 mb-0.5">{chapter}</p>
      <h2 className="text-2xl font-black text-slate-800">{title}</h2>
    </div>
  </div>
);
```

Current scroll useEffect (lines 242–246):
```jsx
useEffect(() => {
  const onScroll = () => setShowScrollTop(window.scrollY > 400);
  window.addEventListener('scroll', onScroll, { passive: true });
  return () => window.removeEventListener('scroll', onScroll);
}, []);
```

Breadcrumb bar (lines 373–386) — this is where the progress bar attaches:
```jsx
<div className="sticky top-0 z-40 bg-white/90 backdrop-blur border-b border-slate-200">
  <div className="max-w-screen-2xl mx-auto px-6 py-3 pl-40 md:pl-8">
    ...breadcrumb nav...
  </div>
</div>
```

Current SectionTitle call sites (all use chapter + title + icon — no color):
- <SectionTitle icon={BookOpen} chapter="Rozdział 0" title="Wstęp" />
- <SectionTitle icon={Users} chapter="Rozdział 1" title="Słownik pojęć i osób" />
- <SectionTitle icon={CheckCircle} chapter="Rozdział 2" title="Przed spotkaniem" />
- <SectionTitle icon={Clock} chapter="Rozdział 3" title="Podczas spotkania" />
- <SectionTitle icon={CheckCircle} chapter="Rozdział 4" title="Po spotkaniu" />
- <SectionTitle icon={FileText} chapter="Rozdział 5" title="Typy protokołów" />
- <SectionTitle icon={AlertTriangle} chapter="Rozdział 6" title="Częste błędy" />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reading progress bar state and scroll listener</name>
  <read_first>
    - src/pages/KompendiumPage.jsx (full file — understand current state declarations and scroll useEffect before touching anything)
  </read_first>
  <files>src/pages/KompendiumPage.jsx</files>
  <action>
    1. After `const [showScrollTop, setShowScrollTop] = useState(false);` (line ~211), add:
       ```jsx
       const [readingProgress, setReadingProgress] = useState(0);
       ```

    2. Replace the existing single-line scroll useEffect:
       ```jsx
       useEffect(() => {
         const onScroll = () => setShowScrollTop(window.scrollY > 400);
         window.addEventListener('scroll', onScroll, { passive: true });
         return () => window.removeEventListener('scroll', onScroll);
       }, []);
       ```
       with this combined version:
       ```jsx
       useEffect(() => {
         const onScroll = () => {
           const scrollTop = window.scrollY;
           const docHeight = document.documentElement.scrollHeight - window.innerHeight;
           setReadingProgress(docHeight > 0 ? Math.round((scrollTop / docHeight) * 100) : 0);
           setShowScrollTop(scrollTop > 400);
         };
         window.addEventListener('scroll', onScroll, { passive: true });
         return () => window.removeEventListener('scroll', onScroll);
       }, []);
       ```

    3. Inside the breadcrumb `<div className="sticky top-0 z-40 bg-white/90 backdrop-blur border-b border-slate-200">`, add the progress bar as the LAST child before the closing `</div>`:
       ```jsx
       <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-slate-100">
         <div
           className="h-0.5 bg-violet-600 transition-all duration-75"
           style={{ width: `${readingProgress}%` }}
         />
       </div>
       ```
       The breadcrumb div already has `border-b border-slate-200` so the progress bar sits flush at the bottom edge of the nav.

    Do NOT touch: checklistState, toggleCheck, IntersectionObserver useEffect, scrollTo, NAV_ITEMS, ALL_SECTION_IDS.
  </action>
  <verify>grep -n "readingProgress" src/pages/KompendiumPage.jsx</verify>
  <acceptance_criteria>
    - `grep -c "readingProgress" src/pages/KompendiumPage.jsx` returns at least 4 (state declaration, setter in onScroll, style prop, useState init)
    - `grep -n "docHeight" src/pages/KompendiumPage.jsx` shows a line with `document.documentElement.scrollHeight - window.innerHeight`
    - `grep -n "h-0.5 bg-violet-600" src/pages/KompendiumPage.jsx` returns at least 1 match
    - `grep -n "kompendium_checklist_v1" src/pages/KompendiumPage.jsx` still returns at least 2 matches (state init + toggleCheck save)
    - `grep -n "passive: true" src/pages/KompendiumPage.jsx` returns exactly 1 match (scroll listener not duplicated)
  </acceptance_criteria>
  <done>Reading progress bar state exists, scroll listener is merged (one listener handles both showScrollTop and readingProgress), progress bar div renders inside breadcrumb bar.</done>
</task>

<task type="auto">
  <name>Task 2: Color-aware SectionTitle and per-section SectionIntro cards</name>
  <read_first>
    - src/pages/KompendiumPage.jsx (lines 354–366 for SectionTitle, lines 465–530 to see existing section JSX structure and all SectionTitle call sites)
  </read_first>
  <files>src/pages/KompendiumPage.jsx</files>
  <action>
    1. Replace the existing `SectionTitle` component definition (lines 354–366) with this color-aware version:
       ```jsx
       const SectionTitle = ({ icon: Icon, chapter, title, color = 'violet' }) => {
         const colors = {
           violet: { bg: 'bg-violet-100', icon: 'text-violet-600', label: 'text-violet-500' },
           blue:   { bg: 'bg-blue-100',   icon: 'text-blue-600',   label: 'text-blue-500'   },
           purple: { bg: 'bg-purple-100', icon: 'text-purple-600', label: 'text-purple-500' },
           teal:   { bg: 'bg-teal-100',   icon: 'text-teal-600',   label: 'text-teal-500'   },
           green:  { bg: 'bg-green-100',  icon: 'text-green-600',  label: 'text-green-500'  },
           amber:  { bg: 'bg-amber-100',  icon: 'text-amber-600',  label: 'text-amber-500'  },
           indigo: { bg: 'bg-indigo-100', icon: 'text-indigo-600', label: 'text-indigo-500' },
           red:    { bg: 'bg-red-100',    icon: 'text-red-600',    label: 'text-red-500'    },
         };
         const c = colors[color] || colors.violet;
         return (
           <div className="flex items-center gap-3 mb-6">
             {Icon && (
               <div className={`w-10 h-10 ${c.bg} rounded-xl flex items-center justify-center shrink-0`}>
                 <Icon className={`w-5 h-5 ${c.icon}`} />
               </div>
             )}
             <div>
               <p className={`text-[10px] font-black uppercase tracking-widest ${c.label} mb-0.5`}>{chapter}</p>
               <h2 className="text-2xl font-black text-slate-800">{title}</h2>
             </div>
           </div>
         );
       };
       ```

    2. Add a new `SectionIntro` component directly after `SectionTitle`:
       ```jsx
       const SectionIntro = ({ items }) => (
         <div className="bg-slate-50 border border-slate-200 rounded-xl p-4 mb-6">
           <p className="text-[10px] font-black uppercase tracking-widest text-slate-400 mb-2">Co znajdziesz w tej sekcji</p>
           <ul className="space-y-1">
             {items.map((text, i) => (
               <li key={i} className="flex items-start gap-2 text-sm text-slate-600">
                 <span className="text-slate-400 shrink-0 mt-0.5">•</span>
                 <span>{text}</span>
               </li>
             ))}
           </ul>
         </div>
       );
       ```

    3. Update all 7 `SectionTitle` call sites in the JSX to add the `color` prop per section:
       - Wstęp (Rozdział 0):   `color="blue"`
       - Słownik (Rozdział 1): `color="purple"`
       - Przed (Rozdział 2):   `color="teal"`
       - Podczas (Rozdział 3): `color="green"`
       - Po (Rozdział 4):      `color="amber"`
       - Typy (Rozdział 5):    `color="indigo"`
       - Błędy (Rozdział 6):   `color="red"`

    4. After each `<SectionTitle ... />` call site, add a `<SectionIntro>` with 2–3 relevant bullet points:
       - Wstęp:   items={['Dlaczego protokół jest ważny dla instytucji', 'Rola protokolanta — co uchwycić, co pominąć', 'Jak korzystać z tego Kompendium']}
       - Słownik: items={['Pełne nazwy organów SSUEW i UEW', 'Tytuły i funkcje osób w protokołach', 'Kiedy używać skrótów, a kiedy pełnych nazw']}
       - Przed:   items={['Checklista czynności przed każdym posiedzeniem', 'Jak przygotować listę obecności i nagłówek', 'Uwagi dla protokołów RUSS i absolutoryjnych']}
       - Podczas: items={['Co zapisywać na bieżąco, a co można uzupełnić po', 'Jak rejestrować głosowania i wnioski formalne', 'Wskazówki dotyczące nagrywania']}
       - Po:      items={['Checklista kontrolna po sporządzeniu protokołu', 'Komu wysłać do weryfikacji i jak archiwizować', 'Co zrobić z nagraniem']}
       - Typy:    items={['Omówienie wszystkich 7 typów protokołów SSUEW', 'Wymagania formalne specyficzne dla każdego typu', 'Terminy i wzory do pobrania']}
       - Błędy:   items={['11 najczęstszych błędów z opisem i sposobem poprawy', 'Błędy dotyczące quorum, dat, głosowań i spójności', 'Jak sprawdzić protokół przed wysyłką']}

    Do NOT modify: BEFORE_ITEMS, AFTER_ITEMS, Checklist component, Accordion component, ProtocolCard component, toggleCheck, sessionStorage logic, NAV_ITEMS, ALL_SECTION_IDS, IntersectionObserver.
  </action>
  <verify>grep -n "SectionIntro" src/pages/KompendiumPage.jsx</verify>
  <acceptance_criteria>
    - `grep -c "color=" src/pages/KompendiumPage.jsx` returns at least 7 (one per SectionTitle call site)
    - `grep -n 'color="blue"' src/pages/KompendiumPage.jsx` returns exactly 1 match (Wstęp section)
    - `grep -n 'color="red"' src/pages/KompendiumPage.jsx` returns exactly 1 match (Błędy section)
    - `grep -c "SectionIntro" src/pages/KompendiumPage.jsx` returns at least 9 (definition + 7 usage sites)
    - `grep -n "Co znajdziesz w tej sekcji" src/pages/KompendiumPage.jsx` returns exactly 1 match (the component definition)
    - `grep -n "kompendium_checklist_v1" src/pages/KompendiumPage.jsx` still returns at least 2 matches (unchanged)
    - `grep -n "bg-violet-100" src/pages/KompendiumPage.jsx` still returns at least 1 match (ProtocolCard still uses violet hardcoded, or SectionTitle colors object violet entry)
  </acceptance_criteria>
  <done>SectionTitle accepts color prop with lookup table for 8 colors; all 7 call sites pass distinct colors; SectionIntro component defined and used in all 7 sections with relevant Polish bullet points; ProtocolCard and checklist components are untouched.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full file is syntactically valid:
```
cd /c/Users/Mikołaj/Downloads/stand-dashboard-main && npx vite build --mode development 2>&1 | head -30
```
Acceptable result: no JSX syntax errors reported. Vite may warn about unused imports — that is acceptable.

Also confirm: `grep -c "kompendium_checklist_v1" src/pages/KompendiumPage.jsx` returns 2 (init + save — persistence untouched per D-05).
</verification>

<success_criteria>
- Reading progress bar: `readingProgress` state declared, merged scroll listener updates it, `<div style={{ width: \`${readingProgress}%\` }}` renders inside breadcrumb (per D-02)
- Colored section headers: SectionTitle has `colors` lookup object with at least 8 entries; 7 call sites each pass a unique color (per D-01)
- Intro cards: SectionIntro component defined and placed after every SectionTitle call site with Polish bullet points (per D-03)
- Checklist persistence unchanged: `kompendium_checklist_v1` key still in toggleCheck save (per D-05)
- No new files created, no src/components/ directory introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-kompendium-ksi-ga-inwentarzowa/04-01-SUMMARY.md`
</output>
